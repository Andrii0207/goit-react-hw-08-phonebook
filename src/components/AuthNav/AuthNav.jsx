import { NavLink } from 'react-router-dom';

export const AuthNav = () => {
  return (
    <div>
      <NavLink to="/registar">Register</NavLink>
      <NavLink to="/login">Register</NavLink>
    </div>
  );
};
